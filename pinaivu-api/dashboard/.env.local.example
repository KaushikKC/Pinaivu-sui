# Dashboard — Copy to .env.local and fill in real values.

GATEWAY_URL=http://localhost:4001
COORDINATOR_URL=
ADMIN_SECRET=

# Get from /setup page after first run
DASHBOARD_ACCOUNT_ID=
DASHBOARD_API_KEY=

# Dev only — remove in production
NODE_TLS_REJECT_UNAUTHORIZED=0
